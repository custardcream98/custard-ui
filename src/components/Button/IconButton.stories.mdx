import { Meta, Canvas, Story } from "@storybook/addon-docs";
import IconButton from "./IconButton";
import { FiArrowUpCircle } from "react-icons/fi";

<Meta
  title="Components/Buttons/IconButton"
  component={IconButton}
/>

export const Template = (args) => <IconButton {...args} />;

# IconButton

`react-icons` 라이브러리에서 제공하는 아이콘을 담을 수 있는 버튼입니다.

<Canvas>
  <Story
    name="Primary"
    args={{
      level: "primary",
      icon: FiArrowUpCircle,
      children: "Primary",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Secondary"
    args={{
      level: "secondary",
      icon: FiArrowUpCircle,
      children: "Secondary",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

텍스트 없이 아이콘만 담을 수도 있으며, 이 경우 `iconTitle` 속성으로 `svg` 요소의 `title` 속성을 설정할 수 있습니다.

<Canvas>
  <Story
    name="Outline"
    args={{
      level: "outline",
      icon: FiArrowUpCircle,
      children: "Outline",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
